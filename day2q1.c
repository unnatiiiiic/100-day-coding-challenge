#include <stdio.h>
int main()
{
int l,b,a,p;
printf("Enter length and breadth of the rectangle ");
scanf("%d",&l);
scanf("%d",&b);
printf("Area= %d ", l*b);
printf("Perimeter= %d ", 2*(l+b));
return 0;
}
